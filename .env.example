# Supabase Configuration
# These values are required for the application to connect to Supabase
# All variables prefixed with VITE_ are PUBLIC and included in the client bundle

# Public Supabase configuration (safe to commit)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-supabase-anon-key
VITE_SUPABASE_PROJECT_ID=your-project-ref

# Optional: Environment Configuration
# NODE_ENV=development
# VITE_APP_VERSION=1.0.0

# SECURITY NOTES:
# - All VITE_ variables are PUBLIC and included in the client bundle
# - Never add private API keys or secrets here
# - For private secrets, use Supabase Edge Functions with the secrets feature
# - Copy this file to .env and fill in your actual values
# - The .env file should not be committed to version control